# KeenInsight 本地数据库监控产品文档

## 1. 文档定位与当前状态

- 产品名称：KeenInsight（新监控产品）
- 重要说明：名称虽然沿用 KeenInsight，但与旧的 KeenInsight 项目和运行实例保持独立
- 文档位置：/root/new/监控产品文档.md
- 监控产品规划目录：/root/new/monitoring
- 产品阶段：论文级、可运行 Demo
- 当前状态：需求收敛中，尚未实施
- 当前未执行：未安装软件、未下载组件、未修改 PostgreSQL、未启动 Prometheus/Grafana

本文档是当前需求基线。任何安装、下载、创建账号、修改配置、创建服务或启动服务的操作，都要先提交实施说明并等待用户明确确认。

## 2. 产品定位

### 2.1 首期目标

先做出一套能运行、能展示、能验证的本地数据库监控 Demo：

- 使用 Prometheus 采集本机 PostgreSQL 监控数据
- 使用 Grafana 展示双语监控仪表盘
- 让用户可以选择某一个数据库进行重点查看
- 同时使用 Prometheus 生态中已有的主机/服务指标；没有现成指标时首期不扩展
- 为后续把监控数据输入 sysinsight 系统调优和 dream SQL 调优预留数据转换链路
- 保留论文展示所需的架构说明、指标说明、实验验证和截图材料

### 2.2 Demo 的“论文级”定义

首期重点是功能完整、过程可复现、数据链路清楚、指标解释明确，适合论文或技术报告演示。首期不追求生产环境级别的高可用、复杂权限、安全加固和自动化运维。

### 2.3 核心原则

- 不修改旧项目 /root/keeninsight-postgres，除非之后得到明确授权
- Prometheus、Grafana、数据库 exporter 和主机 exporter 使用成熟开源项目的源码或官方开源发行物
- 不从零独立编写 Prometheus、Grafana 或 exporter 的替代实现
- 允许编写独立的格式转换脚本，把监控数据转换成 sysinsight 或 dream 所需格式
- 格式转换脚本只负责读取、清洗、映射和输出，不负责系统调优、SQL 调优或自动修改数据库
- 首期先完成本机监控；远程实例和 MySQL 放到后续阶段

## 3. 已确认需求

| 事项 | 已确认内容 |
|---|---|
| 产品名称 | KeenInsight |
| 旧项目关系 | 新产品独立于旧 KeenInsight，不修改旧项目 |
| 首期监控对象 | 本机 PostgreSQL 实例 |
| 数据库选择方式 | 让用户从数据库列表中选择某一个数据库进行重点监控 |
| 主机/服务指标 | Prometheus 生态已有指标就使用，没有现成指标首期不自研 |
| 后续数据库 | MySQL，普通业务场景 |
| 监控核心 | Prometheus + Grafana |
| 开源要求 | 使用开源代码源码或官方开源发行物，不自行重写监控核心 |
| 指标保留时间 | 24 小时 |
| 仪表盘 | 需要预置数据库监控仪表盘 |
| 仪表盘语言 | 中文和英文双语 |
| 后续数据流向 | 监控产品输出监控数据，作为 sysinsight 和 dream 的输入 |
| 数据适配方式 | 允许通过格式转换脚本进行转换 |
| 后续系统 | sysinsight 系统调优、dream SQL 调优 |
| 首期安全目标 | 暂不处理生产级安全、HTTPS、反向代理和复杂权限 |

## 4. 首期 Demo 范围

### 4.1 PostgreSQL 监控

首期发现本机 PostgreSQL 实例中的数据库，并在 Grafana 中提供数据库选择入口。用户选择一个数据库后，重点查看该数据库的运行情况。

优先展示以下指标：

- PostgreSQL 和 exporter 是否正常
- 当前数据库连接数、活跃连接和连接使用率
- 事务提交、回滚和吞吐变化
- 活跃查询、等待会话、长事务
- 锁等待、阻塞和死锁
- 查询耗时和查询调用次数
- pg_stat_statements 能提供的查询统计
- 缓存命中率、读写 I/O、临时文件和临时空间
- WAL、checkpoint、后台写入
- vacuum/autovacuum 状态
- 数据库大小和容量变化

表级、索引级、复制、日志采集等内容首期不作为硬性要求；如果开源 exporter 已经直接提供，可以作为展示增强项。

### 4.2 本机资源监控

只使用 Prometheus 生态中已有的采集能力，优先展示：

- CPU 使用率和负载
- 内存使用情况
- 文件系统容量
- 磁盘 I/O
- 网络流量

如果已有开源 exporter 能提供进程、端口或其他系统指标，可以按 Demo 展示价值选择加入；不为此单独开发采集器。

### 4.3 其他本机服务

KeenInsight 应用、Kafka、Python 服务等不作为首期必做目标。只有在 Prometheus 生态中已有合适 exporter 或现成指标时，才考虑加入存活状态或资源面板。

### 4.4 Grafana 仪表盘

首期建议提供以下双语仪表盘：

1. PostgreSQL 实例总览
2. 数据库选择与连接/事务
3. 查询、锁与等待
4. I/O、WAL、checkpoint 与 vacuum
5. 数据库容量与主机资源

数据库选择使用 Grafana 变量实现；仪表盘中显示当前选择的实例、数据库和时间范围。

## 5. 开源组件方案

以下组件作为实施阶段的优先候选，当前尚未安装：

| 能力 | 开源组件 | 作用 |
|---|---|---|
| 指标采集和时序存储 | Prometheus | 拉取 exporter 指标并保留 24 小时 |
| 可视化 | Grafana OSS | 展示查询结果、仪表盘和告警状态 |
| PostgreSQL 指标 | postgres_exporter（Prometheus Community） | 输出 PostgreSQL 指标 |
| 主机指标 | node_exporter（Prometheus Community） | 输出 CPU、内存、磁盘、网络等指标 |

具体版本和安装来源在实施方案中列出。优先使用各项目公开源码仓库或官方开源发行物，并记录版本、来源和校验信息。

## 6. 数据流与后续调优对接

### 6.1 总体数据流

本机 PostgreSQL
→ postgres_exporter
→ Prometheus
→ Grafana 双语仪表盘

Prometheus 查询结果
→ 格式转换脚本
→ sysinsight 系统调优

Prometheus 查询结果
→ 格式转换脚本
→ dream SQL 调优

监控产品是数据源，sysinsight 和 dream 是后续数据使用方。首期先完成 Prometheus/Grafana 监控，后续再接入转换脚本和目标系统。

### 6.2 sysinsight 系统调优

预期输入：

- CPU、内存、磁盘、网络趋势
- PostgreSQL 连接、事务、锁等待、I/O
- WAL、checkpoint、vacuum 状态
- 数据库容量和时间窗口

数据链路：

Prometheus 查询结果 → 格式转换脚本 → sysinsight 输入

格式转换脚本不执行系统调优，只负责生成 sysinsight 能读取的数据。

### 6.3 dream SQL 调优

预期输入：

- queryid 或查询指纹
- 规范化 SQL 或允许提供的 SQL 文本
- 调用次数、总耗时、平均耗时、最大耗时
- 返回行数、读写块、临时空间
- 数据库名、时间窗口和应用来源
- 后续如有明确需求，再提供执行计划、表结构和索引信息

数据链路：

Prometheus 查询结果 → 格式转换脚本 → dream 输入

格式转换脚本不自动改写 SQL、不建索引、不执行 SQL。

### 6.4 首期对接边界

- 首期可以只完成转换脚本的接口和样例数据，不强行连接真实的 sysinsight/dream
- 真实对接需要目标系统提供输入格式、字段说明和样例
- 没有目标系统接口资料时，Demo 可以先输出本地 JSON/CSV 样例文件，证明数据链路可用
- 首期不执行自动系统调优或自动 SQL 调优

## 7. 首期重点指标与告警建议

首期以“页面可见、可解释、可验证”为主，告警先在 Prometheus/Grafana 中展示，不配置复杂通知链路。

### 7.1 P0 指标

- PostgreSQL 可用性
- exporter 和 Prometheus 采集状态
- 当前数据库连接数和连接使用率
- 锁等待、阻塞和死锁
- 磁盘空间

### 7.2 P1 指标

- 事务提交/回滚和吞吐
- 活跃查询、长事务和查询耗时
- 缓存命中率
- 数据库读写 I/O
- WAL、checkpoint、vacuum/autovacuum
- CPU、内存和磁盘 I/O

### 7.3 Demo 默认告警规则建议

阈值在实施方案中作为可调整配置提交确认，初步建议：

- PostgreSQL 或 exporter 采集失败
- 连接使用率持续超过 80%
- 出现持续锁等待或长事务
- 死锁次数增加
- 磁盘使用率超过 80%/90%
- CPU、内存或磁盘 I/O 持续较高
- 查询耗时相对基线明显升高

## 8. Demo 交付和验收目标

### 8.1 功能验收

- Prometheus 能采集本机 PostgreSQL 指标
- Grafana 能正常连接 Prometheus
- 能显示数据库列表，并选择一个数据库进行重点查看
- 双语仪表盘可以展示连接、事务、查询、锁、I/O、容量和主机资源
- 指标能够持续产生并保留 24 小时
- 至少一组异常状态能在页面或告警规则中体现
- 不影响旧 KeenInsight 项目和现有数据库业务

### 8.2 论文/技术报告材料

Demo 建议同步形成：

- 系统架构图
- 组件和开源来源说明
- PostgreSQL 指标定义表
- Prometheus 采集配置说明
- Grafana 仪表盘截图
- 正常状态和异常状态的对比实验
- 监控数据到 sysinsight/dream 的转换流程图
- Demo 的限制和后续扩展说明

## 9. 分阶段路线

### 阶段 0：Demo 需求和实施方案

- 合并用户填写的答案
- 确认少量关键参数
- 提交开源组件版本、目录结构和变更清单

### 阶段 1：本机监控 Demo

- 在 /root/new/monitoring 建立配置和运行文件
- 部署 Prometheus、Grafana、PostgreSQL exporter 和可用的主机 exporter
- 接入本机 PostgreSQL
- 导入/整理双语仪表盘
- 验证数据库选择、指标采集和 24 小时保留

### 阶段 2：格式转换 Demo

- 从 Prometheus 查询监控数据
- 形成统一的中间数据结构
- 输出 sysinsight 和 dream 的样例输入
- 根据目标系统资料再适配真实接口

### 阶段 3：真实系统对接

- 对接 sysinsight 系统调优
- 对接 dream SQL 调优
- 记录调优前后的监控指标变化

### 阶段 4：扩展

- 支持远程 PostgreSQL
- 支持 MySQL 普通业务场景
- 按数据库类型接入对应成熟开源 exporter

## 10. 首期明确不做

为了保持 Demo 聚焦，以下内容先不纳入首期：

- HTTPS、反向代理、公网暴露和复杂访问控制
- 密钥管理、细粒度权限、多租户
- 高可用、集群部署和生产级容灾
- 告警邮件、企业微信、钉钉、飞书等通知链路
- 远程数据库和 MySQL 的实际接入
- 自动系统调优、自动改写 SQL、自动建索引和自动执行 SQL
- 完整日志平台、日志检索和链路追踪
- 复杂的备份、升级、回滚和卸载系统

## 11. 精简后的待确认问题（请直接填写）

只保留会影响论文级 Demo 设计和后续对接的问题，其余事项按 Demo 默认方案处理。

### Q-001 pg_stat_statements 使用范围

是否同意使用 PostgreSQL 已有或可用的 pg_stat_statements 查询统计，作为 dream SQL 调优的基础数据？

回答：可以，后续如果不足的话，还需要再增加

### Q-002 SQL 数据形式

提供给 dream 的 SQL 数据，首期希望使用完整 SQL、规范化 SQL，还是 queryid/查询指纹加统计指标？

回答：完整的，全部的SQL数据，都可以给，没有隐私一说

### Q-003 执行计划

首期是否需要把执行计划纳入 dream 的输入？如果没有明确需要，默认先不采集。

回答：需要给

### Q-004 sysinsight 输入资料

请提供 sysinsight 的输入格式、字段说明、样例数据，以及它接收文件/API/命令的方式；如果暂时没有资料，请填写“先输出本地样例”。

回答：https://github.com/Avalephh/KeenInsight/这个git里面有sysinsight的文件分支

### Q-005 dream 输入资料

请提供 dream 的输入格式、字段说明、样例数据，以及它接收文件/API/命令的方式；如果暂时没有资料，请填写“先输出本地样例”。

回答：https://github.com/Avalephh/KeenInsight/这个git里面有dream的文件分支

### Q-006 格式转换 Demo 的输出方式

在没有真实接口资料时，是否先默认输出 JSON 样例文件，同时可选输出 CSV，作为两个调优系统的输入示例？

回答： 可以的

### Q-007 格式转换脚本的运行方式

Demo 阶段格式转换脚本是否先采用手动执行，确认数据正确后再增加定时执行？

回答：  不是，这个格式转换，是持续进行的，比如触发了调优，那你就需要拉取一段时间的数据，格式转换一下给后续的步骤，是这样

### Q-008 Prometheus 采集间隔

是否同意 Demo 先采用 15 秒采集间隔；如果采集开销或数据粒度不合适，再调整为 5 秒或 30 秒？

回答：可以，你可以多试试

### Q-009 开源组件安装形态

是否允许使用各开源项目官方发布的二进制发行物；如果你要求必须从源码编译，请明确说明。无特别要求时，我会在方案中同时列出源码和官方发行物来源。

回答：允许

### Q-010 双语仪表盘形式

双语是否先采用中文和英文两套仪表盘，避免首期增加复杂的语言切换逻辑？

回答：允许

### Q-011 论文 Demo 材料

是否需要我在 Demo 完成后同时整理架构图、指标说明、部署过程、实验步骤、正常/异常截图和结果分析？

回答：可以

### Q-012 首期是否真实连接调优系统

阶段 1 是否只完成本地监控，阶段 2 先生成 sysinsight/dream 的本地样例输入，等接口资料齐全后再做真实连接？

回答：可以的，你也可以看一下https://github.com/Avalephh/KeenInsight/仓库里面怎么获取数据的

## 12. 决策记录

| 编号 | 事项 | 决定 | 日期 |
|---|---|---|---|
| D-001 | 先进行需求梳理 | 通过对话和文档集中确认，暂不实施部署 | 2026-09-09 |
| D-002 | 产品名称 | 新监控产品继续使用 KeenInsight 名称，但与旧项目隔离 | 2026-09-09 |
| D-003 | 首期监控范围 | 本机 PostgreSQL；用户选择某一个数据库进行重点监控 | 2026-09-09 |
| D-004 | 主机/服务指标 | Prometheus 生态已有指标就使用，没有现成指标首期不自研 | 2026-09-09 |
| D-005 | 后续数据库 | MySQL，普通业务场景 | 2026-09-09 |
| D-006 | 数据保留 | 保留 24 小时 | 2026-09-09 |
| D-007 | 仪表盘 | 预置数据库监控仪表盘，中文/英文双语 | 2026-09-09 |
| D-008 | 对接数据流 | 监控产品输出数据，通过格式转换脚本作为 sysinsight/dream 的输入 | 2026-09-09 |
| D-009 | Demo 定位 | 首期做论文级可运行 Demo，暂不追求生产级安全和运维完整性 | 2026-09-09 |

## 13. 答案处理规则

1. 用户直接在 Q-001 至 Q-012 后填写答案。
2. 我会根据答案删除不再需要的设计分支，形成简短实施方案。
3. 实施方案会单列所有将要安装、创建、修改和启动的内容。
4. 只有用户明确确认实施方案后，才开始实际操作。
